[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scry"
version = "0.2.0"
description = "Eval orchestrator for HuggingFace models — the divinatory counterpart to Merlina (Schneewolf Labs)."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Schneewolf Labs" }]
requires-python = ">=3.10"
keywords = ["evaluation", "benchmarks", "llm", "vlm", "schneewolf-labs", "lm-eval-harness", "bfcl"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
# FastAPI server: POST /eval, job queue, WebSocket progress
api = ["fastapi>=0.110", "uvicorn>=0.27", "websockets>=12.0"]
# lm-eval-harness backend for LLM evaluation
harness = [
    "lm_eval>=0.4.0",
    "transformers>=4.35.0",
    "accelerate>=0.24.0",
]
# httpx powers fastapi.testclient; API tests skip themselves without it
test = ["pytest>=7.0", "httpx>=0.24"]

[project.scripts]
scry-eval = "scry.cli:main"
scry-server = "scry.server:main"

[project.urls]
Homepage = "https://github.com/Schneewolf-Labs/Scry"
Repository = "https://github.com/Schneewolf-Labs/Scry"
Issues = "https://github.com/Schneewolf-Labs/Scry/issues"

[tool.setuptools.packages.find]
include = ["scry*"]
exclude = ["tests*"]
